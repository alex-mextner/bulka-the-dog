# Telegram Instant View template for https://bulka.rs/
# Submission: paste at https://instantview.telegram.org/contest
# Docs:       https://instantview.telegram.org/docs
#
# Bare-minimum baseline. IV 2.1 requires only `title` and `body`. We start
# with that and add @remove cleanups incrementally — adding too many at
# once made the editor get stuck on "Processing links…" indefinitely
# (Telegram fetches metadata for every inline link in body before rendering;
# the more rules, the more passes; aggressive @remove with overlapping
# selectors can also stall preview generation).

~version: "2.1"

title: //h1[@id="hero-title"]
body:  //main[@id="main"]
